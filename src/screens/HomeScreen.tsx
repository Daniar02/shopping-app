import React from 'react'
import {SafeAreaView, ScrollView, Text} from 'react-native'

const HomeScreen = () => {
  return (
    <ScrollView>
      <SafeAreaView>
        <Text>Hello Daniar</Text>
      </SafeAreaView>
    </ScrollView>
  );
};

export default HomeScreen;
